{
  "name": "design-artifact-loop",
  "version": "1.2.0",
  "description": "Render-grounded, non-slop UI design artifacts: commit a design system before markup, then iterate against a deterministic linter and an independent vision critic.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "bun test server/",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun build server/index.ts --target=node --outfile=server/dist/index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
